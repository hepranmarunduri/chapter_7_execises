# Store cubes between 1 to 10 to a list.
# Cube is a number raised to the third power.
# Print it using a for loop.

numbers = list(range(1, 11))

for number in numbers:
	cube = number ** 3
	print(cube)